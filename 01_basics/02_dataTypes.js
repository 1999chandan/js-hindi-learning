"use strict";  // if we write "use strict" it will treat js code as newer version.

// alert(3 +3) //  it will through an errror while using in node js but it will work on browser console 

// premitive data types:-
   // 1. number => 2 to power 53 
   // 2. bigint
   // 3. string => "valuse inside doulbe coutes"
   // 4. boolean => true/false
   // 5. null => standalone value but when we try to console type of null it will give type "Object"
   // 6. undefined => 
   // 7. symbol ==> unique

// object

console.log(typeof null); // output will be "object"
console.log(typeof undefined); // output will be undefined